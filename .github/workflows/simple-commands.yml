name: Simple Commands
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Echo World
        run: echo "Hello, World!"

      - name: Show Date
        run: date

      - name: List Files
        run: ls -la